# specify what happens at startup.

on startup:
	let this_game = game.all.first
	print this_game.name
	print "by " + this_game.author
	if this_game.release == final:
		print "version " + this_game.version 
	else:
		print "version " + this_game.version + " " + this_game.release